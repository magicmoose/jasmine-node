#!/usr/bin/env node

if(!process.env.NODE_ENV ){
    process.env.NODE_ENV = 'test';
}

var path = require('path');
var fs   = require('fs');
require('./../lib/jasmine-node/cli2.js').interpret(process.argv);
